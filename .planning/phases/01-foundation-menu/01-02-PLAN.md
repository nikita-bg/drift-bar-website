---
phase: 01-foundation-menu
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/menu-data.ts
  - app/api/menu/route.ts
autonomous: true
requirements: [DATA-01, MENU-02, MENU-03]

must_haves:
  truths:
    - "Menu data is typed and accessible via TypeScript interface"
    - "API route returns menu data as JSON"
    - "All 6 categories exist (signatures, classic, wine, whiskey, beer, soft)"
    - "Each menu item has id, name, price in EUR, and description"
  artifacts:
    - path: "lib/menu-data.ts"
      provides: "Typed menu data structure migrated from menu.js"
      exports: ["MenuCategory", "MenuItem", "MENU"]
      min_lines: 150
    - path: "app/api/menu/route.ts"
      provides: "GET endpoint returning menu JSON"
      exports: ["GET"]
      min_lines: 10
  key_links:
    - from: "app/api/menu/route.ts"
      to: "lib/menu-data.ts"
      via: "import MENU"
      pattern: "import.*MENU.*from.*menu-data"
---

<objective>
Migrate menu data from static JavaScript to typed TypeScript structure and create API route for dynamic access.

Purpose: Convert the hardcoded menu data from website/menu.js into a properly typed, server-accessible data structure that can be easily updated without redeployment.

Output: Typed menu data file with all 6 categories and 40+ items, plus a GET API route returning menu JSON.
</objective>

<execution_context>
@C:/Users/Nikita/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nikita/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-menu/01-01-SUMMARY.md

Existing menu data source:
@website/menu.js (lines 14-144 contain MENU object with all categories and items)
</context>

<interfaces>
<!-- Menu data structure from website/menu.js -->

Menu categories structure:
```javascript
const MENU = {
  signatures: {
    title: 'Авторски',
    titleAccent: 'Сигнатури',
    subtitle: 'Селекция 2026',
    type: 'featured',
    items: [
      {
        id: 'sig-1',
        name: 'Purple Haze',
        price: 14.00,
        desc: 'Description text',
        tags: ['Floral', 'Gin Base'],
        image: 'https://...'
      }
    ]
  },
  classic: {
    title: 'Класически',
    titleAccent: 'Коктейли',
    subtitle: null,
    type: 'simple',
    items: [
      { id: 'cls-1', name: 'Old Fashioned', price: 14.00, desc: 'Ingredients' }
    ]
  }
  // ... wine, whiskey, beer, soft categories
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create typed menu data structure</name>
  <files>lib/menu-data.ts</files>
  <action>
    Create lib/menu-data.ts with TypeScript interfaces and data:

    1. Define MenuItem interface:
       - id: string
       - name: string
       - price: number (in EUR)
       - desc: string
       - tags?: string[] (optional, only for signatures)
       - image?: string (optional, only for signatures)

    2. Define MenuCategory interface:
       - title: string
       - titleAccent: string
       - subtitle: string | null
       - type: 'featured' | 'simple'
       - items: MenuItem[]

    3. Define Menu type as Record<CategoryKey, MenuCategory> where CategoryKey = 'signatures' | 'classic' | 'wine' | 'whiskey' | 'beer' | 'soft'

    4. Export const MENU: Menu object with all data migrated from website/menu.js (lines 14-144).
       - Keep exact same structure and content
       - Convert JavaScript object to TypeScript with proper types
       - Maintain all 6 categories: signatures (6 items), classic (8 items), wine (5 items), whiskey (7 items), beer (6 items), soft (7 items)
       - Preserve Bulgarian text exactly as-is
       - Keep all prices in EUR (already in EUR in source)

    Use `as const satisfies Menu` for type safety while maintaining literal types.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    TypeScript compilation succeeds, no type errors, MENU export is properly typed.
  </verify>
  <done>Menu data migrated to TypeScript with proper interfaces, all 6 categories and 40+ items present</done>
</task>

<task type="auto">
  <name>Task 2: Create menu API route</name>
  <files>app/api/menu/route.ts</files>
  <action>
    Create app/api/menu/route.ts with GET handler:

    ```typescript
    import { NextResponse } from 'next/server';
    import { MENU } from '@/lib/menu-data';

    export async function GET() {
      return NextResponse.json(MENU);
    }
    ```

    This API route:
    - Returns full menu object as JSON
    - Sets appropriate Content-Type header (NextResponse handles this)
    - Returns 200 status by default
    - Is cacheable (consider adding revalidation in future if menu updates frequently)

    Export GET function only (no POST needed for Phase 1).
  </action>
  <verify>
    <automated>curl http://localhost:3000/api/menu | head -20</automated>
    API returns JSON with menu categories, first item shows correct structure.
  </verify>
  <done>API route created, returns menu data as JSON, accessible at /api/menu</done>
</task>

</tasks>

<verification>
Run these checks after task completion:
1. `npx tsc --noEmit` — No TypeScript errors
2. `npm run dev` then `curl http://localhost:3000/api/menu` — Returns full menu JSON
3. Check JSON response includes all 6 categories with correct item counts
4. Verify prices are numbers (not strings) and descriptions are in Bulgarian
</verification>

<success_criteria>
- Menu data typed with TypeScript interfaces (MenuItem, MenuCategory, Menu)
- All 6 categories migrated from menu.js to menu-data.ts
- 40+ menu items present with id, name, price, desc fields
- Prices in EUR (numeric format)
- API route /api/menu returns complete menu JSON
- TypeScript compilation passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-menu/01-02-SUMMARY.md`
</output>
