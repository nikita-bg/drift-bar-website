---
phase: 01-foundation-menu
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/menu/page.tsx
  - app/menu/components/MenuHeader.tsx
  - app/menu/components/CategoryTabs.tsx
  - app/menu/components/MenuGrid.tsx
  - app/menu/components/MenuItem.tsx
  - app/menu/menu.module.css
autonomous: false
requirements: [MENU-04, MENU-05]

must_haves:
  truths:
    - "Customer can view menu by navigating to /menu"
    - "Menu displays all categories in tabs"
    - "Menu items show name, description, and price in EUR"
    - "Menu matches vintage aesthetic of landing page"
    - "Menu is responsive on mobile devices (320px to 768px)"
  artifacts:
    - path: "app/menu/page.tsx"
      provides: "Menu page with client-side interactivity"
      exports: ["default"]
      min_lines: 40
    - path: "app/menu/components/MenuHeader.tsx"
      provides: "Header with back button and table indicator"
      exports: ["default"]
      min_lines: 20
    - path: "app/menu/components/CategoryTabs.tsx"
      provides: "Category navigation tabs"
      exports: ["default"]
      min_lines: 25
    - path: "app/menu/components/MenuGrid.tsx"
      provides: "Grid container for menu items"
      exports: ["default"]
      min_lines: 15
    - path: "app/menu/components/MenuItem.tsx"
      provides: "Individual menu item card"
      exports: ["default"]
      min_lines: 30
    - path: "app/menu/menu.module.css"
      provides: "Vintage-styled menu CSS"
      min_lines: 100
  key_links:
    - from: "app/menu/page.tsx"
      to: "/api/menu"
      via: "fetch on component mount"
      pattern: "fetch.*['\"]/?api/menu['\"]"
    - from: "app/menu/components/MenuItem.tsx"
      to: "app/menu/menu.module.css"
      via: "CSS module import"
      pattern: "import.*styles.*from.*menu\\.module\\.css"
    - from: "app/menu/page.tsx"
      to: "app/menu/components/*"
      via: "component composition"
      pattern: "import.*from.*components"
---

<objective>
Build responsive menu UI components matching vintage aesthetic of landing page, with category navigation and mobile-first layout.

Purpose: Create the customer-facing digital menu interface that displays menu items from the API, matches the established design system, and works perfectly on mobile devices (primary use case).

Output: Working /menu page with header, category tabs, menu item grid, and vintage styling that matches website/index.html aesthetic.
</objective>

<execution_context>
@C:/Users/Nikita/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nikita/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-menu/01-01-SUMMARY.md

Design reference (DO NOT modify, only reference for styling):
@website/menu.html (lines 22-62 for structure reference)
@website/menu.css (for vintage styling patterns — grain, sepia, parchment)
@website/style.css (for design system tokens already extracted to globals.css)
</context>

<interfaces>
<!-- Types from Plan 01-02 menu-data.ts -->
```typescript
interface MenuItem {
  id: string;
  name: string;
  price: number;
  desc: string;
  tags?: string[];
  image?: string;
}

interface MenuCategory {
  title: string;
  titleAccent: string;
  subtitle: string | null;
  type: 'featured' | 'simple';
  items: MenuItem[];
}

type Menu = Record<'signatures' | 'classic' | 'wine' | 'whiskey' | 'beer' | 'soft', MenuCategory>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create menu page with data fetching</name>
  <files>app/menu/page.tsx</files>
  <action>
    Create app/menu/page.tsx as a Client Component ('use client' directive):

    1. State management:
       - selectedCategory: string (default: 'signatures')
       - menuData: Menu | null (fetched from /api/menu)
       - tableNumber: string | null (read from URL query param ?table=N)

    2. Data fetching:
       - useEffect hook to fetch from /api/menu on mount
       - Store in menuData state
       - Show loading state while fetching

    3. Layout structure:
       - MenuHeader component (table number display, back button)
       - Hero section with "НАШЕТО МЕНЮ" heading (reference menu.html lines 38-48)
       - CategoryTabs component (passes selectedCategory, onCategoryChange handler)
       - MenuGrid component (passes current category items)

    4. Responsive meta:
       - Set viewport in layout.tsx (already done in Plan 01)
       - Mobile-first approach (works 320px+)

    Use TypeScript with proper Menu types imported from @/lib/menu-data.
    DO NOT implement cart functionality (Phase 2).
  </action>
  <verify>
    <automated>npm run build</automated>
    Build succeeds, page is statically optimized where possible, client component works.
  </verify>
  <done>Menu page created with data fetching, category state management, and component composition</done>
</task>

<task type="auto">
  <name>Task 2: Build menu UI components</name>
  <files>app/menu/components/MenuHeader.tsx, app/menu/components/CategoryTabs.tsx, app/menu/components/MenuGrid.tsx, app/menu/components/MenuItem.tsx</files>
  <action>
    Create four components (all Client Components):

    **MenuHeader.tsx:**
    - Props: tableNumber?: string
    - Back button (arrow_back icon) linking to /
    - Brand name "DRIFT BAR"
    - Table indicator (table_restaurant icon + table number or "—")
    - Sticky positioning on scroll
    - Reference website/menu.html lines 22-35 for structure

    **CategoryTabs.tsx:**
    - Props: categories: string[], activeCategory: string, onCategoryChange: (cat: string) => void
    - Horizontal scrollable tabs (6 tabs: Сигнатури, Класика, Вино, Уиски, Бира, Безалкохолни)
    - Active tab highlighted with vintage accent color (--color-accent)
    - Mobile: horizontal scroll, no wrap
    - Reference website/menu.html lines 51-58

    **MenuGrid.tsx:**
    - Props: items: MenuItem[], categoryType: 'featured' | 'simple'
    - Grid layout: 1 column mobile, 2 columns tablet+
    - Renders MenuItem components for each item
    - Handles empty state (no items)

    **MenuItem.tsx:**
    - Props: item: MenuItem, featured: boolean
    - Featured items (signatures): Show image, tags, larger card
    - Simple items: Text-only, compact card
    - Display: name (bold), description (smaller text), price (EUR with € symbol)
    - Vintage card styling: parchment background, subtle shadow, hover state
    - Material Symbols icons for tags (if featured)

    Use CSS modules (menu.module.css) for styling.
    Use next/image for featured item images (optimize with proper sizes).
  </action>
  <verify>
    <automated>npm run dev && curl http://localhost:3000/menu</automated>
    Menu page loads, all 6 category tabs visible, switching tabs shows different items.
  </verify>
  <done>All menu UI components created, featured vs simple item layouts implemented, components properly typed</done>
</task>

<task type="auto">
  <name>Task 3: Style with vintage aesthetic CSS</name>
  <files>app/menu/menu.module.css</files>
  <action>
    Create app/menu/menu.module.css with vintage design matching website/menu.css patterns:

    1. **Layout:**
       - Mobile-first grid (1 col → 2 col at 768px)
       - Proper spacing using CSS variables from globals.css
       - Sticky header with backdrop blur

    2. **Vintage aesthetic:**
       - Parchment background (--color-parchment) for cards
       - Sepia tones for text (--color-sepia, --color-ink)
       - Subtle box shadows (warm tone, not harsh black)
       - Film grain overlay on featured cards (use ::before pseudo-element)
       - Rounded corners (8px) for cards
       - Border: 1px solid rgba sepia tone

    3. **Typography:**
       - Font family: var(--font-primary) (Space Grotesk from layout)
       - Item names: 600 weight, 1.1rem size
       - Descriptions: 400 weight, 0.9rem size, --color-sepia
       - Prices: 700 weight, 1rem size, --color-accent (gold)

    4. **Category tabs:**
       - Horizontal scroll container (overflow-x: auto)
       - Active tab: border-bottom 3px solid --color-accent
       - Tab buttons: uppercase, letter-spacing, smooth transition
       - Vintage paper texture background

    5. **Responsive:**
       - Mobile (320px-767px): 1 column, tabs scroll horizontally
       - Tablet+ (768px+): 2 columns, tabs centered
       - Desktop (1024px+): max-width container, larger cards

    6. **Interactions:**
       - Card hover: slight lift (translateY -2px), shadow increase
       - Tab click: smooth category transition
       - Touch-friendly tap targets (min 44px height)

    Reference website/menu.css for exact color values and grain animation patterns. Extract and adapt, do NOT copy entire file.
  </action>
  <verify>
    Manual verification required (checkpoint in verification section).
  </verify>
  <done>Vintage aesthetic CSS applied, responsive layout works 320px to desktop, matches landing page visual style</done>
</task>

</tasks>

<verification>
Automated checks:
1. `npm run build` — Build succeeds without errors
2. `npm run dev` — Dev server starts
3. Visit http://localhost:3000/menu — Page loads

Manual checkpoint required after automated checks pass.
</verification>

<success_criteria>
- Menu page accessible at /menu route
- All 6 category tabs displayed and functional
- Clicking tabs filters menu items correctly
- Menu items show name, description, price in EUR
- Featured items (signatures) show images and tags
- Simple items (classic, wine, etc.) show text-only cards
- Vintage aesthetic matches landing page (grain, sepia, parchment)
- Responsive layout works on mobile (320px), tablet (768px), desktop (1024px+)
- Space Grotesk font applied correctly
- Table number displays in header if ?table=N query param present
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-menu/01-03-SUMMARY.md`

Then create checkpoint task:

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Responsive digital menu UI with vintage aesthetic:
    - Menu header with table number indicator
    - 6 category tabs (Сигнатури, Класика, Вино, Уиски, Бира, Безалкохолни)
    - Menu item cards (featured with images, simple text-only)
    - Vintage styling (grain, sepia, parchment, Space Grotesk font)
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Open http://localhost:3000/menu in mobile view (Chrome DevTools, 375px width)
    3. Verify visual aesthetic matches landing page (grain overlay, sepia tones, parchment cards)
    4. Click through all 6 category tabs — confirm items update correctly
    5. Check featured items (Сигнатури) show images and tags
    6. Check simple items (Класика, Вино, etc.) show compact text cards
    7. Verify prices display in EUR format (e.g., "14.00 €")
    8. Test responsive: resize from 320px (mobile) to 768px (tablet) to 1024px+ (desktop)
    9. Confirm Space Grotesk font is loaded (not default system font)
    10. Add ?table=5 to URL — verify table number appears in header
  </how-to-verify>
  <resume-signal>
    Type "approved" if visual aesthetic matches landing page and all categories display correctly.
    Describe any issues if adjustments needed (colors off, spacing wrong, fonts incorrect, responsive breaks).
  </resume-signal>
</task>
</output>
