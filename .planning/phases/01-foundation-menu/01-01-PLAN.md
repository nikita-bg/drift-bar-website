---
phase: 01-foundation-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.js
  - tsconfig.json
  - app/layout.tsx
  - app/globals.css
  - .gitignore
autonomous: true
requirements: [MENU-01]

must_haves:
  truths:
    - "Next.js dev server starts without errors"
    - "App Router structure is initialized with /app directory"
    - "TypeScript compilation works"
    - "Space Grotesk font loads correctly"
  artifacts:
    - path: "package.json"
      provides: "Next.js dependencies and scripts"
      contains: "next"
    - path: "next.config.js"
      provides: "Next.js configuration"
      min_lines: 5
    - path: "app/layout.tsx"
      provides: "Root layout with fonts and metadata"
      exports: ["default"]
    - path: "tsconfig.json"
      provides: "TypeScript configuration for Next.js"
      contains: "compilerOptions"
  key_links:
    - from: "app/layout.tsx"
      to: "next/font/google"
      via: "Space_Grotesk import"
      pattern: "import.*Space_Grotesk.*from.*next/font/google"
    - from: "package.json"
      to: "next build"
      via: "build script"
      pattern: '"build":\\s*"next build"'
---

<objective>
Initialize Next.js 14 App Router project with TypeScript and vintage aesthetic foundation.

Purpose: Establish the technical foundation for migrating the existing static menu.html to a Next.js application that matches the landing page aesthetic.

Output: Working Next.js dev environment with proper TypeScript config, Space Grotesk font, and vintage design system CSS variables.
</objective>

<execution_context>
@C:/Users/Nikita/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nikita/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

Existing assets to reference (NOT modify):
- website/menu.html — Target aesthetic reference
- website/style.css — Landing page vintage aesthetic (sepia, grain, Space Grotesk)
- website/menu.css — Menu-specific styling to migrate
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with TypeScript</name>
  <files>package.json, next.config.js, tsconfig.json, .gitignore</files>
  <action>
    Run `npx create-next-app@latest . --typescript --app --no-src-dir --import-alias "@/*"` to initialize Next.js 14 with App Router in the current directory.

    Configure next.config.js:
    - Enable image optimization domains if needed
    - Set basePath to empty (root deployment)
    - Add any Vercel-specific optimizations

    Update .gitignore to include:
    - .next/
    - node_modules/
    - .env*.local
    - .vercel/

    Ensure tsconfig.json has App Router paths configured correctly.
  </action>
  <verify>
    <automated>npm run dev</automated>
    Dev server starts on port 3000 without TypeScript errors.
  </verify>
  <done>Next.js project initialized, TypeScript configured, dev server runs successfully</done>
</task>

<task type="auto">
  <name>Task 2: Setup root layout with vintage aesthetic foundation</name>
  <files>app/layout.tsx, app/globals.css</files>
  <action>
    Create app/layout.tsx with:
    - Space Grotesk font loaded via next/font/google (weights: 400, 500, 600, 700, 800)
    - Metadata: title "Drift Bar Plovdiv — Меню", description "Дигитално меню на Drift Bar Plovdiv"
    - Bulgarian language (lang="bg")
    - Viewport meta for mobile (width=device-width, initial-scale=1.0, maximum-scale=1.0)

    Create app/globals.css with CSS variables matching existing landing page aesthetic:
    - Color palette: --color-parchment (#f4efe4), --color-sepia (#8b7355), --color-ink (#2b2520), --color-accent (#d4af37)
    - Typography: --font-primary (Space Grotesk)
    - Spacing scale: --space-xs through --space-xl
    - Film grain overlay CSS (@keyframes grain animation)
    - Mobile-first base styles (font-size, line-height)

    Import Material Symbols Outlined font from Google Fonts CDN (used for icons in menu.html).

    Reference website/style.css for exact color values and aesthetic patterns. DO NOT copy-paste entire file — extract only design system variables and base resets.
  </action>
  <verify>
    <automated>npm run build</automated>
    Build completes successfully, CSS variables are accessible, Space Grotesk font loads.
  </verify>
  <done>Root layout created with correct font, metadata, and vintage design system CSS variables established</done>
</task>

</tasks>

<verification>
Run these checks after task completion:
1. `npm run dev` — Dev server starts without errors
2. `npm run build` — Production build succeeds
3. Open http://localhost:3000 — See default Next.js page with Space Grotesk font
4. Inspect CSS variables in browser DevTools — Confirm --color-parchment, --color-sepia, etc. are defined
</verification>

<success_criteria>
- Next.js 14 App Router project initialized with TypeScript
- Dev server runs on localhost:3000
- Production build completes successfully
- Space Grotesk font loads correctly
- Vintage aesthetic CSS variables defined and accessible
- Project ready for menu components and data integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-menu/01-01-SUMMARY.md`
</output>
